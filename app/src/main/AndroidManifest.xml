<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chirinos.mejoresmaestros">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddPublicador"
            android:label="@string/nameAddPublicador"
            android:theme="@style/Theme.Dialog.Publicador" />
        <activity
            android:name=".Sala1Activity"
            android:label="@string/nameSala1" />
        <activity
            android:name=".Sala2Activity"
            android:label="@string/nameSala2" />
        <activity
            android:name=".EditAsignacionesActivity"
            android:label="@string/nameEditar"
            android:theme="@style/Theme.Dialog.Publicador" />
        <activity android:name=".VerActivity"></activity>
    </application>

</manifest>